const userState = {
    currentUser: false
};

export default function userReducer(prevState = userState, action)
{
    switch (action.type) {
        case '':
            
            break;
    
        default:
            return prevState;
    }
}